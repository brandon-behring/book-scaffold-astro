# sources/manifest.yaml — canonical index of every external source cited.
#
# Template placeholder: the two entries below (gwern-sidenote, tufte-css)
# are preserved because `_example-chapter.mdx` cites them to demonstrate
# Citation resolution. Replace them with your book's real sources before
# your first real chapter ships.
#
# Each entry is a single citable source. The `id` is a slug used as the
# citation key in chapter MDX (e.g. <Citation src="my-source" />).
#
# The full Playwright capture of each source lives in sources/cache/<hash>/
# and is gitignored (for drift detection only — never published). Public-
# facing pages only render the metadata below plus the Perma.cc link.
#
# build-bib compiles this manifest to src/data/sources.json (v4.10.0); the
# auto-injected /references page renders those sources grouped by tier.
#
# Tier scale:
#   T1-official        — vendor-official documentation or release notes
#   T2-release-notes   — release blog or changelog (less formal than docs)
#   T3-practitioner    — respected community writing with durable argument
#   T4-conjecture      — blog posts, tweets, or unverified claims
#
# Validation: see `sources` collection in src/content.config.ts.
# Methodology: see ~/.claude/skills/book-scaffold-astro/pedagogy/source-tiers.md

- id: gwern-sidenote
  url: https://gwern.net/sidenote
  title: "Sidenotes In Web Design"
  author: Gwern Branwen
  publish_date: 2020-02-01
  captured_at: 2026-04-17T13:00:00Z
  tier: T3-practitioner
  tool: cross-tool
  perma_cc: null
  local_cache: null

- id: tufte-css
  url: https://edwardtufte.github.io/tufte-css/
  title: "Tufte CSS"
  author: Dave Liepmann
  publish_date: 2014-01-01
  captured_at: 2026-04-17T13:00:00Z
  tier: T3-practitioner
  tool: cross-tool
  perma_cc: null
  local_cache: null
