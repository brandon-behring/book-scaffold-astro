# Roadmap — `book-scaffold-astro`

**Original date:** 2026-06-14 · **Refreshed:** 2026-07-14 · **Original baseline:** v4.24.0 · **Published baseline:** v5.2.0 · **Status:** active

This is the post-reconciliation refresh tracked by #185. It preserves the
original roadmap's consumer-signal and design-before-code reasoning, while
absorbing the tracker work filed through #212, the v4.26 release repair, the
v4.27 stabilization batch, the production-verified #187 performance fix, the
four post-stabilization slices (#143, #147, #190, and #161/#164), the v5.0.0
corpus/configuration tranche, the v5.1.0 readiness release, and the v5.2.0
per-page social-card release. No implementation slice remains selected; the
final portfolio follow-through is complete, and all open work is explicitly
parked on a trigger.

Status terms in this document are deliberate:

- **Shipped** means the change is on `main`, tagged, and published in both
  lock-step npm packages.
- **Completed follow-through** means cross-repository or provider-owned work is
  verified and closed without implying a scaffold package release.
- **Backlog / parked / on signal** means intentionally unscheduled until its
  recorded trigger fires; it is not forgotten work and does not belong in the
  Project `Todo` queue.

## Executive state

- **v4.26.3 is shipped.** The hotfix restores `releaseStatus` inheritance
  through composed Styles, preserves top-level precedence, and adds an explicit
  `false` opt-out (#149). The `v4.26.3` tag is on `main`, both npm packages are
  published at 4.26.3, and the hotfix milestone is closed.
- **v4.27.0 stabilization is shipped.** All 15 milestone issues (#154,
  #175–#177, #179–#184, #186, #188, and #206–#208) closed with release
  receipts; the 11 source PRs (#191–#200 and #202) closed as superseded after
  their corrected integration. The tag and `main` point at `8c5ed1b`, both npm
  packages are published at 4.27.0, and the milestone is closed.
- **v4.27.1 performance fix is shipped.** #187 isolated the reported CLS 0.274
  to a late Roboto swap, changed only the package-owned body face to
  `font-display: optional`, and preloaded its common Latin variable asset. The
  `ssm-foundations` production canary kept all 24 normal/delayed-font samples
  under 0.1 CLS; both packages are published at 4.27.1.
- **v4.28.0 demo substrate is shipped.** #143 exports only the reusable frame,
  control, metric-card, and theme-color primitives plus explicit `demo.css`;
  domain kernels and opinionated visualizations remain parked in #103. The
  final packed-artifact consumer canary passed 109 companion tests.
- **v4.29.0 cross-book validation is shipped.** #147 adds backward-compatible
  sibling descriptors, route-aware Astro heading indexes, and exact literal
  path/fragment validation. A disposable four-book matrix indexed 893 headings
  plus 17 component labels and resolved all seven authored fragment links.
- **v4.30.0 authored-link containment is shipped.** #190 structurally validates
  Markdown, raw HTML, and static MDX `href`/`src` targets against the evaluated
  Astro base. It reports safe normalized suggestions, never rewrites content,
  and has no cosmetic opt-out for links that still resolve from the host root.
- **v4.31.0 figure authoring is shipped.** #161/#164 add manifest-owned
  Warm–Tol semantic roles, separate Okabe–Ito data-series colors, versioned SVG
  palette migration, accessible/base-aware inline figures, and a transparent
  vector-first authoring standard with light/dark proof.
- **v5.0.0 is shipped.** #80 adds the manifest-backed,
  namespaced corpus route/schema/tooling/search contract; #211 removes inert
  `deploy` configuration; and #212 requires an explicit preset. A two-book
  research-portfolio fixture proves root and subpath routing, per-book data and
  apparatus isolation, and Pagefind book filters. PR #220 merged to `main`,
  tag `v5.0.0` published both packages, and a fresh registry consumer passed.
- **v5.1.0 is shipped.** #158 adds deterministic human and schema-v1 readiness
  output, per-book results plus an explicit shared corpus aggregate, exit codes
  0/1/2, and offline `init-qa` schema support. PR #222 merged to `main`; tagged
  publish run 29289404745 released both packages; 732 package tests, 32
  `create-book` tests, all five generated presets, and fresh registry canaries
  passed.
- **v5.2.0 is shipped.** #157 adds opt-in deterministic 1200×630 social cards
  after final static rendering, with exact page/corpus/static precedence,
  corpus identity, root/subpath URLs, local fonts, exclusions, deduplication,
  and fail-loud output containment. PR #224 merged as `f22ceae`; tagged publish
  run 29292640530 released both packages. The 750-test toolkit suite, 32
  `create-book` tests, 23 focused OG gates, visual/demo CI, and packed offline
  builds for all five presets passed.
- **Post-v5.2 tracker snapshot (2026-07-14):** 7 open issues, 0 open PRs, and
  0 open milestones. No P1/P2/P3 implementation is active; all seven issues
  are intentionally parked in Work Tracker Project `Backlog` with an owner,
  review date, and evidence-based resume trigger.

## Delivery after the original v4.24.0 baseline

| Release | State | Outcome |
|---|---|---|
| v4.25.0 | Shipped | Section map and margin/template apparatus (#150). |
| v4.25.1 | Shipped | Base-aware injected route pages and non-route emitters (#142). This also resolves the scaffold-owned half later reported in #168; authored prose links remain #190. |
| v4.25.2 / v4.25.3 | Partial, superseded | The toolkit-only artifacts exist on npm, but neither has a matching `create-book` version or release tag, so neither is a valid D12 lock-step release. The reconciled contents reached both packages in v4.26.2; do not target the partial versions. |
| v4.26.0 | Shipped | Book-aware navigation and the mobile drawer: a useful #80 slice, but not full multibook corpus routing. |
| v4.26.1 | Tagged, not published | The reconciled source and ancestry/concurrency/dry-run guards landed on `main` (#178), and visual baselines were repaired (#169), but the registry publish did not complete. The version was superseded rather than reused. |
| v4.26.2 | Shipped | First registry artifact from the reconciled line, including the responsive work; also added site-wide `releaseStatus` configuration (#149/#201) and correct research-portfolio Part numbering (#203). |
| v4.26.3 | Shipped | Immediate `releaseStatus` Style-composition hotfix: last-defined replacement, explicit suppression, top-level precedence, and observable virtual-config tests (#149). |
| v4.27.0 | Shipped | Stabilization batch: shared base handling, configured/self-healing authoring tools, default security headers, complete five-preset scaffolding, dual licenses/author metadata, turnkey PDF, consumer-owned VersionSelector data, and reconciled guides (#154, #175–#177, #179–#184, #186, #188, #206–#208). |
| v4.27.1 | Shipped | Prevented late body-font reflow with a narrow Roboto optional/preload policy, added delayed-font viewport gates, and production-canary verified #187. |
| v4.28.0 | Shipped | Graduated the opt-in interactive-demo substrate without domain kernels, including accessible controls, theme-aware SVG helpers, reduced-motion behavior, and packed consumer proof (#143). |
| v4.29.0 | Shipped | Added backward-compatible vendored sibling indexes, route-aware Astro heading labels, and exact cross-book path/fragment validation across a four-book consumer matrix (#147). |
| v4.30.0 | Shipped | Added validation-only, structurally parsed containment for authored Markdown/HTML/MDX targets under a non-root Astro base, including decoded literals and dot-segment-safe suggestions (#190). |
| v4.31.0 | Shipped | Added manifest-owned semantic and categorical figure palettes, versioned SVG migration, accessible/base-aware inline figures, and the vector-first authoring standard (#161, #164). |
| v5.0.0 | Shipped | First-class manifest-backed corpora, book-scoped routes/data/search/tooling, mandatory explicit presets, and removal of inert `deploy` configuration (#80, #211, #212), verified by tagged CI and a fresh registry consumer. |
| v5.1.0 | Shipped | Deterministic single-book and corpus readiness through `qa`/`init-qa`, exact route/anchor checks, local JSON Schema validation, generated-project wiring, and all-five-preset consumer proof (#158). |
| v5.2.0 | Shipped | Opt-in build-time social cards with page/corpus/static precedence, five deterministic themes, base-aware content-addressed PNGs, structural HTML patching, and packed offline proof (#157). |

Other reconciliation outcomes in the #154–#178 range are now explicit:

- #162 (mobile display-math reflow) and #163 (chrome-free landing support) are
  shipped; #169's related visual baselines are current.
- #167 is closed as a duplicate of the canonical cross-book target-validation
  issue #147, with its four-book consumer evidence preserved there.
- #168's scaffold-rendered link defect was already fixed in v4.25.1; its
  authored-MDX remainder is split cleanly to #190.
- #174 duplicated the XRef JSDoc break fixed in v4.9.0; affected consumers need
  a version bump, not a second code fix.
- #178 is closed after reconciling tag, branch, registry, and `main` ancestry.

## Shipped — v4.27.0 stabilization

v4.27 is the contract-hardening release that corrected the review backlog and
filled live distribution gaps without taking the two intended v5 breaks. The
table records the shipped result and its integrated delivery path.

| Issue | Accepted v4.27 result | Final delivery state |
|---|---|---|
| #154 | Make the demo homepage a truthful root and non-root-base exemplar; remove dead example destinations and prove emitted links in both builds. | Shipped via corrected #192 integration and hosted root/subpath builds. |
| #175 | Add optional per-kind theorem counters while retaining the existing single theorem sequence by default. | Shipped via corrected #200 integration. |
| #176 | Have `validate` flag authored theorem-like `n=` values that disagree with the number computed into `labels.json`, including numeric literal handling. | Shipped via corrected #199 integration. |
| #177 | Export the shared `ssmMacros` KaTeX macro set from the main package entry and keep the recipe aligned with the real preset-resolution path. | Shipped via corrected #193 integration. |
| #179 | Preserve the legacy implicit `minimal` fallback for v4 compatibility, but make it visible with one deprecation warning and consistently resolve explicit config, environment, and `.env` sources. Strict absence becomes #212. | Shipped via corrected #196 integration. |
| #180 | Mark the inert `deploy` surface deprecated and warn only when a consumer explicitly supplies the unsupported top-level option. Do not pretend built-in Style metadata changes deployment. Removal becomes #211. | Shipped via corrected #191 integration. |
| #181 | List all five presets in help/errors and pin generated self-documentation to the exact toolkit release rather than `v3.0`, `main`, or another floating target. | Shipped via corrected #194 integration. |
| #182 | Replace the repeated `BASE_URL` regex idioms with shared parameter-taking `normalizeBase` / `baseNoSlash` helpers and prove canonical root/subpath output. | Shipped via #198 integration. |
| #183 | Cover the drawer's focus/ESC/backdrop/scroll-lock/keyboard contract, preset-resolution branches, and fatal exits for every build-script dispatcher path. | Shipped via expanded #197 integration and 92-pass fixture suite. |
| #184 | Correct public/internal documentation drift, enforce the D12 dual-guide contract (`CLAUDE.md` canonical, `AGENTS.md` pointer), remove stale manifest/CI claims, and verify packed/generated guides. | Shipped via corrected #195 integration and tarball contracts. |
| #186 | Let `validate` transparently regenerate missing `labels.json` and `references.json` before checking them, while keeping generated-book lifecycle hooks. | Shipped via corrected #202 integration. |
| #188 | Generate a safe default Cloudflare `_headers` policy unless disabled, allow only a CSP override, preserve a consumer-owned `public/_headers`, and cover the integration hook itself. | Shipped from the integration pass with real demo artifacts. |
| #206 | Put scoped MIT software and CC BY 4.0 content licenses in both npm packages and every generated book; capture `--author` in either CLI form with `Book contributors` as the neutral default. | Shipped from the distribution pass and both packed artifacts. |
| #207 | Restore a turnkey `npm run pdf`: build first, serve the preview, render `/print/` through Paged.js, and smoke a non-empty academic PDF. | Shipped with npm 12 script approval and hosted/local PDF receipts. |
| #208 | Remove fictional VersionSelector destinations and the automatic mount; retain a compatible, exported, prop-driven component that renders nothing without consumer data. Real automatic data remains #209. | Shipped from the integration pass; automatic data remains parked. |

The release gate was intentionally stronger than “all PRs merged.” The tagged
tree passed package build/tests/type checks, `create-book` tests, root and
subpath demo builds, generated-book smoke, npm pack-content checks for both
packages, real local and hosted PDF rendering, and gallery interaction/visual
coverage. Toolkit and CLI remain identical at 4.27.0; trusted-publish run
29259302086 verified both registry artifacts.

## Completed cross-repository follow-through

The portfolio review resolved the final rollout/watch queue without a toolkit
release. #83 and #160 closed on production evidence; #159 moved to the parked
table because its second-live-sibling trigger has not fired.

| Issue | Verified outcome | Disposition |
|---|---|---|
| #83 | The shipped v4.8.0 provenance contract now renders evidence-backed disclosures across all current chapters in Double ML (10/10), `ssm-foundations` (17/17), `dlai-study-notes` (18/18, including the 12 backfilled gaps), and `claude-books` (100/100, including the independent audit on all 30 Architect Reference chapters). `guides` has no non-fixture chapter corpus; its first real chapter is a future consumer-local trigger rather than residual scaffold work. | Closed complete after both consumer deployments and representative live-route checks; no scaffold package release. |
| #160 | Cloudflare's zone-level edge injection loaded the same valid beacon on all eight public surfaces; each script returned 200 and each same-origin `POST /cdn-cgi/rum` returned 204 without an analytics CSP failure. | Closed not planned because working provider-owned injection supersedes a scaffold-specific injection API. |

## Parked / on signal — review 2026-10-15

Every intentionally parked item has an owner, a concrete trigger, and the same
review date. A calendar review does not itself authorize implementation; it
checks whether the trigger has fired.

| Issue | Parked scope | Resume trigger |
|---|---|---|
| #103 | Domain kernels and opinionated visualization primitives (`OLS`, residualization, sampling simulations, `ScatterFit`, `SamplingDistribution`). The stable substrate shipped separately in v4.28.0 through #143. | Two independent shipped consumers expose compatible signatures and fixtures for one stable API. |
| #144 | Rendering and interaction semantics for the schema-reserved cloze question type. | A non-fixture consumer supplies representative cloze content plus its grammar, reveal, scoring, accessibility, and no-JavaScript contract. |
| #145 | Deriving flashcards from questions and cross-mapping them by objective/domain. Weighted exam sampling is out of scope. | A consumer supplies front/back expectations for each requested question type plus real objective/domain mappings. |
| #146 | Native academic equation numbering and reference semantics; no implementation plugin is preselected. | A non-fixture manuscript supplies numbered equations, labels, references, error behavior, accessibility, and print expectations. |
| #159 | Shared sibling discovery and family → hub → portfolio navigation. Existing `portfolio`, `siblingBooks`, `BookLink`, and #147 validation remain the baseline. | A family has a second live sibling and supplies canonical ownership, ordering, destinations, and current-site identity. |
| #209 | A real multi-version metadata source and deployment wiring for the prop-driven `VersionSelector`. | A consumer operates two concurrently live versions and supplies canonical/current, retention, base-path, and corpus URL semantics. |
| #210 | An `<AnkiCard>` authoring primitive and tool-neutral `extract-cards` JSON. Direct `.apkg` generation remains downstream. | A second independent consumer proves a compatible workflow and schema alongside the historical 154-card, one-deck-plus-JSON DLAI pilot. |

**Owner:** @brandon-behring for all seven. **Review date:** 2026-10-15.

## v5.0 corpus gate — shipped

### Direction retained: corpus as monorepo (#80)

The independent-consumer trigger fired: `guides-ai-engineering` joined the
DLAI study-notes pilot as a second single-app, multi-guide consumer. The chosen
direction remains **one app, one build, multiple namespaced books**. It
graduates the way both consumers already operate instead of asking them to
federate independent deployments.

The decision-complete contracts are recorded in
[`v5-corpus-contract.md`](v5-corpus-contract.md),
[`qa-contract.md`](qa-contract.md), and
[`og-card-contract.md`](og-card-contract.md). The corpus contract preserves
Recipe 21's `/chapters/<book>/<slug>/` public URLs, selects one homogeneous
preset/Style chain per Astro app, derives book identity from entry ids, and
defines the exact route, artifact, diagnostic, Pagefind, and migration model.
The v5.0 implementation follows the corpus contract, v5.1.0 implements the QA
contract, and v5.2.0 implements the OG-card contract.

The cost model from the original roadmap still matters:

- Route injection is centralized in `ROUTE_REGISTRY`; adding book-aware route
  patterns is not principally a dispatcher rewrite.
- Profile schemas are selected independently; this is not principally a Zod
  union rewrite.
- The expensive surface is book-scoping the build products and validation:
  labels/counters, exercises, tips, references, route parameters, links,
  readiness counts, and search/navigation across a shared corpus.
- Federation remains an unfired, separate need. It would require a corpus-wide
  search index, landing/selector behavior, and deployment/base-prefix rules that
  neither signaling consumer requested.

The v4.26 book-aware navigation release was a no-regret slice of #80. v5.0
completes the remaining contract by implementing:

1. the corpus/book manifest and profile-composition model;
2. route identities and `getStaticPaths` parameters for corpus landing,
   per-book chapter indexes, chapters, and apparatus routes;
3. book-scoped output keys and diagnostics for every build/validate command;
4. Pagefind indexing, result URLs, and navigation at both book and corpus level;
5. single-book compatibility and migration from recipe 21's `generateId`
   workaround; and
6. how #147, #158, and #190 compose with the corpus contract without being
   falsely treated as already implemented by it.

#158 stayed intentionally **outside** the v5.0 milestone. Its interface,
metrics, selectors, and exit codes were designed with #80, then shipped in
v5.1.0 against the completed book resolver. #157 followed independently in
v5.2.0 with per-book social-card identity.

### Two deliberate v5 removals

| Issue | v4.27 compatibility bridge | v5 contract |
|---|---|---|
| #211 | `deploy` remains typed but is deprecated; an explicit unsupported top-level value warns. | Implemented: removed from book/style types and built-in Styles; retained values fail with a migration diagnostic. Deployment continues to be selected by real scaffold/deploy configuration. |
| #212 | Missing preset still falls back to `minimal` with one deprecation warning; explicit config/env/`.env` values are validated. | Implemented: an explicit valid preset is required, and absence or conflict fails with an actionable diagnostic. |

These removals ship in the same v5 migration story as #80, but they do not
expand #80's implementation. #210 remains explicitly outside the multibook
scope.

## Sequencing guardrails

1. v5.0.0, v5.1.0, and v5.2.0 are published and verified; no implementation
   slice is currently selected.
2. Keep Project `Todo` for selected delivery work. `Backlog` items move there
   only after their recorded evidence trigger fires.
3. Preserve the v4 deprecation and migration record for the two intentional
   v5 breaks.
4. Prioritize demonstrated consumer failure and shared duplication over small
   speculative features. Effort is a planning input, not the ordering rule.
5. Keep stable demo plumbing separate from volatile domain APIs.
6. Do not invent family navigation, cloze, equation-reference,
   VersionSelector, or card-extraction contracts before their recorded
   consumer evidence exists.
7. Treat the accepted #80 scope and migration plan as the compatibility
   boundary; amend it explicitly before changing corpus route/tooling behavior.

## Tracker map at refresh

- **Shipped/closed in the absorbed range:** #162, #163, #167 (duplicate of
  #147), #168 (remainder split to #190), #169, #174 (duplicate of #120), and
  #178; v4.26.3 closes the reopened #149 regression.
- **v4.27 milestone (15, closed):** #154, #175–#177, #179–#184, #186, #188,
  #206–#208; all shipped in v4.27.0.
- **v5 milestone (3, shipped/closed):** #80, #211, #212.
- **Post-v5 shipped/closed:** #158 in v5.1.0 and #157 in v5.2.0.
- **Post-v5 follow-through, closed:** #83 (completed consumer provenance) and
  #160 (provider-owned analytics superseded scaffold injection).
- **Project Backlog / parked on signal:** #103, #144–#146, #159, #209, and
  #210; owner @brandon-behring, review 2026-10-15.

This partition accounts for all 7 remaining open issues, 0 open PRs, and no
open milestones. Work Tracker Project #1 puts all seven in `Backlog`; `Todo`
is reserved for selected delivery. The `status:parked` / `trigger:on-signal`
labels preserve the same distinction in issue views.
